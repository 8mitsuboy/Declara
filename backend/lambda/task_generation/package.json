{
  "name": "declara-task-generation-lambda",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --target=node20 --outfile=dist/index.js",
    "check": "biome check .",
    "format": "biome format --write .",
    "test": "node --test"
  },
  "devDependencies": {
    "@aws-sdk/client-ssm": "^3.1039.0",
    "@biomejs/biome": "^2.0.0",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  }
}
